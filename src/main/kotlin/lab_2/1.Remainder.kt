package lab_2

fun main() {
//    val divisible = readln().toInt()
//    val divisor = readln().toInt()
    val result = 20%17
    if (result > 0) {
        println("Получилось значение большее, чем ноль!")
    }
}